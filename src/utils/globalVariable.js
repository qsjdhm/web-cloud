/**
 * 全局变量,例如：表单提交的字数限制，变淡宽度
 * */

// * name一般是30
// * no一般是30
// * code一般是30
// * 数字长度一般是9位
// * 备注一般255
export default {
    textarea: 250,
    string: 30,
    number: 9,
    /* 弹窗dialog */
    dw_col: '430px', // 弹窗的宽度，一列input输入框
    dw: '800px', // 弹窗的宽度，仅仅是在两列input输入框
    dw2: '850px', // 弹窗中嵌套table中，table中的显示2行列
    dw3: '900px', // 弹窗中嵌套table中，table中的显示2行列
    dw4: '950px', // 弹窗中嵌套table中，table中的显示2行列
    dw5: '1000px', // 弹窗中嵌套table中，table中的显示2行列

    fixed_l: 200, // 左侧列固定宽度
    fixed_r_p1: 100, // 右侧列固定宽度
    fixed_r_p1_2: 120,
    fixed_r_p2: 200,
    fixed_r_p3: 300,
    fixed_r_p4: 500,
    /* 操作栏 ，根据按钮实现宽度 */
    op_1: 80,
    op_2: 100,
    op_3: 140,
    op_4: 200,
}
